# MalTracer runtime + build dependencies
# Install with:  pip install -r requirements.txt

# ── Core monitoring / detection ──
psutil>=5.9
watchdog>=3.0
beautifulsoup4>=4.12

# ── Secure credential storage ──
keyring>=24.0

# ── Gmail scanner (OAuth2 + Gmail API) ──
google-auth>=2.0
google-auth-oauthlib>=1.0
google-api-python-client>=2.0

# ── Native desktop GUI ──
PySide6>=6.6

# ── Packaging + tests (dev only) ──
pyinstaller>=6.0
pytest>=7.0
